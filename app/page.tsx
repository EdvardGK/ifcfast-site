/**
 * ifcfast.com — the landing page is the Scene + Instrument experience
 * (chapters 01–05 over a real point cloud, settling into the viewer).
 * The measured receipts page lives at /receipts.
 */
export { default } from "./mockups/ab/page";
