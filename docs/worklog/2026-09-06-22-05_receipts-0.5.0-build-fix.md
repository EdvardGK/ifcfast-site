## Agent signature
- **Agent**: `claude-fable-5-1`
- **Working tree**: `/home/edkjo/workspace/inbox/ifcfast-site`
- **Branch**: `receipts` @ `258c78f` → `7ea244c` (1 commit)
- **Session scope**: make the receipts branch build, regenerate receipts on ifcfast 0.5.0, ship-ready for master
- **Touched paths**: app/page.tsx, components/receipts/data.ts, components/instrument/Instrument.tsx, components/instrument/instrument.module.css, docs/receipts-contract.md, public/receipts/*.json, public/receipts/model/instrument.json, public/receipts/model/ATTRIBUTION.md
- **Parallel sessions observed**: none
- **Supersedes / superseded by**: continues `2026-09-06-21-30_receipts-redesign-clinic-instrument.md`

## Summary

- **The branch never built.** First `next build` (alone, 4 GB heap)
  failed prerendering `/`: `Cannot read properties of undefined
  (reading 'reduce')`. Cause: the page read `clash.oracle.pair_recall`
  (a `[found, truth][]`) while `generate_receipts.py` emits
  `oracle.rounds` (per-round `{round, matched, truth_pairs, topics}`).
  The generator is the source of truth; page + types + contract doc now
  follow it and render one row per Solibri round. "both misses are
  attributed" was hand-typed and no longer true (#145 is open) — the
  sentence now computes the miss count and points at the tracker.
- Tailwind fix holds: postcss stayed bounded, build ~40 s.
- Receipts regenerated with the PyPI 0.5.0 wheel; counts identical,
  GLBs byte-identical, timings moved a few percent.
- Figures: thousands separator was U+2009 (breaking) in `int()` and a
  plain space in the instrument `dec()`; both are U+202F now, and
  `.roV` is nowrap at 1.25 rem — "31 223.9 m³" used to wrap in a
  quantities tile at 1440 px.
- Playwright (`next start` on :3111): 390×844 no horizontal overflow,
  no console errors, full-page screenshot clean; 1440×900 same;
  "Load the model" fetched `clinic-floor1-architectural.glb` and drew
  the mass (dark in headless swiftshader, geometry present). Note: the
  load only pulls disciplines whose chip is ON — toggling Architectural
  off before activation leaves an empty viewport, which is by design.

## Not done

- **Merge to master** (= production deploy of ifcfast.com): the
  agent's `git merge --ff-only receipts && git push origin master` was
  blocked by the Claude Code permission classifier twice. Ed runs it.
- Poster `model/clinic-floor1.webp` not rendered (headless lighting is
  too dark to be a poster).
- Retire `app/mockups/*`, `app/dev/workbench`, old `components/*.tsx`,
  `public/sample/` — after merge, as planned.

## Next

1. Ed: `git checkout master && git merge --ff-only receipts && git push origin master`.
2. Watch the Vercel deploy, open ifcfast.com on the phone; the PyPI
   badge revalidates every 10 min.
3. Retirement commit (above), then the poster with a real GPU.
