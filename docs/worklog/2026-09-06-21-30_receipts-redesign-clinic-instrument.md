## Agent signature
- **Agent**: `claude-fable-5-1`
- **Working tree**: `/home/edkjo/workspace/inbox/ifcfast-site`
- **Branch**: `receipts` @ `ebf62f4` (master) → `b180425` + uncommitted receipts/GLBs/Tailwind fix (see Next)
- **Session scope**: ifcfast.com redesign — phone-first "receipts" landing + the chapter-06 instrument, fed by real Medical-Dental Clinic data
- **Touched paths**: app/page.tsx, app/globals.css, app/layout.tsx (fonts), components/receipts/*, components/instrument/*, public/receipts/**, docs/receipts-contract.md
- **Parallel sessions observed**: a sprucelab session (pytest + uvicorn) ran on the same box during this work; parser-side commits `1867a69`, `66cb5e8`, `3baeb30` on EdvardGK/ifcfast are this session's
- **Supersedes / superseded by**: supersedes the July "film → instrument" direction in 2026-07-17-16-30_ifcfast-com-accuracy-and-immersive-redesign.md for the page spine; keeps its chapter-06 instrument

## Summary

Owner brief: "pretty shit as is and useless on phones, and the example
gives no credit to the abilities." Audit (screenshots at 390 px): live
page scrolls sideways, 17 355 px of desktop dashboard tiles, 3.4 MB
eager JSON; the July AB mockup draws text over the model at every
width. Both showcase the 289-product Duplex.

Decision (argued in the sounding-board analysis, adopted): every
section is a **receipt** — one claim, the number that proves it, the
command that reproduces it — from one real multi-discipline building.
Owner then picked the AB **chapter-06 instrument** as the federation
section's 3D block (and wants it later in sprucelab: sprucelab #699).
So: light receipt page as the spine; graphite instrument as the device
inside it; phone-first stacking; selection by tap (not drag) keyed on
glTF node `extras.guid`.

Data: buildingSMART Medical-Dental Clinic, CC BY 4.0, five disciplines
(121.6 MB, 37 772 products). Generated by
`ifcfast/scripts/generate_receipts.py` (nothing hand-typed):
cold open 0.76 s all five; federate 1.2 s + clash 2.6 s → 36 425
cross-model pairs (9 531 hard `clash`); QTO vs ifcopenshell per class
with `reference_exceeds_aabb` (coverings: 250/250 rows where the
oracle exceeds the element's own AABB — the reference is the wrong
side); subset 0.24 s, hotswap 0.29 s touching 53/209 k records,
mutate 0.4 s; MCP 23 tools; first-floor GLB per discipline (ARK 1.5,
STR 1.6, HVAC 1.7, EL 0.2, **PL 12.0 MB** → opt-in toggle until
ifcfast #170) + `instrument.json` (6 771 products, 3 381 with clash
partners).

Parser work this needed, shipped on ifcfast main: mixed-unit
federation (#169, `3baeb30`) — Clinic authors 3 models in m and 2 in
mm; `federate()` now rescales to the finest unit.

Page (agent build, branch `receipts`): 236 KB first load on a phone,
zero horizontal overflow at 390/1440, three.js only after tap, IBM
Plex, one accent, contrast ≥ 4.5:1. Preview:
https://ifcfast-site-8u9log6z0-spruceforge.vercel.app (Vercel-auth
protected; state = commit `b180425`, placeholders for clash/write/mcp).

## Evidence on the crashes (five session exits today)

1. 14:48, 17:20 — kernel OOM on the corpus pytest lane (parser side).
2. 20:23, 20:40, 20:56 — kernel OOM on **`next`'s PostCSS worker**
   (`.next/build/postcss.js`, 3 → 7 GB in 30 s, memwatch log).
   Cause: Tailwind 4 auto-source scanning read `public/receipts/*.glb`
   (12 MB binary) as text once the real GLBs were copied in. Fixed in
   `app/globals.css`: `@import "tailwindcss" source(none)` +
   `@source "../app"; @source "../components";` — UNCOMMITTED, UNBUILT.
3. The Claude session itself also exited twice without any OOM; the
   transcript is 7 MB+. Treat this session as unrecoverable; continue
   in a fresh one.

## Next (fresh session, in order)

1. `cd ifcfast-site && git status` — expect: modified `app/globals.css`
   (Tailwind fix), real `public/receipts/*.json`, `model/instrument.json`,
   five `model/clinic-floor1-*.glb`. Verify the JSON matches
   `ifcfast/scratch/clinic/receipts_out/` (regenerate with
   `python scripts/generate_receipts.py --clinic .local-samples/clinic --work scratch/clinic --out ../ifcfast-site/public/receipts`
   if in doubt — 4 min, Python only).
2. Build ALONE: `NODE_OPTIONS=--max-old-space-size=4096 npm run build`.
   Watch RSS of `.next/build/postcss.js`; it must stay < 1 GB now.
3. `npm run start`, Playwright at 390×844 and 1440×900: overflow check,
   tap-to-load Architectural, tap a wall → receipt panel; toggle HVAC;
   confirm Plumbing shows its 12 MB before loading. Render a real
   poster: screenshot the loaded viewport → `model/clinic-floor1.webp`.
4. Commit (GLBs included; 17 MB is fine for this repo), push `receipts`
   → Vercel preview → send Ed the link + phone/desktop screenshots.
   Merge to `master` only on Ed's go (production auto-deploys).
5. Retire `app/mockups/*`, `app/dev/workbench`, old `components/*.tsx`,
   `public/sample/` in a follow-up commit after merge.
6. Parser follow-ons filed: #168 (RIV fan/coil oracle divergence),
   #170 (adaptive circle tessellation → Plumbing GLB budget), sprucelab
   #699 (adopt the instrument).
